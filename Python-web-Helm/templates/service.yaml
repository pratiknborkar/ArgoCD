apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-web.fullname" . }}
  labels:
    app: {{ include "python-web.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
  selector:
    app: {{ include "python-web.name" . }}
