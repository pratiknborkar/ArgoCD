replicaCount: 2

image:
  repository: iad.ocir.io/yourtenancy/yourrepo/nginx
  tag: stable
  pullPolicy: IfNotPresent
  pullSecrets:
    - name: ocir-secret  # your image pull secret

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
